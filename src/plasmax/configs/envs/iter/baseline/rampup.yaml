# ITER baseline — ramp-up phase, constrained 3 MA cold anchor.
#
# Cold start; only the 0–60 s Ip ramp (3 → 15 MA) is simulated. See base.yaml
# for the physics-gap caveats.
#
# Geometry:  Per-Ip FreeGSnke EQDSKs (3 / 4.5 / 15 MA), the ramp-up segment.
#            Episode: t_final = 60 s → 600 steps.

phase: rampup
reset_reference: iter_baseline_cold_3ma

initialization: ${DATA_DIR}/initializations/iter/baseline/cold.yaml

task:
  reward: lh_transition
  terminal_penalty: -100

torax:
  profile_conditions:
    Ip: {0: 3.0e+6, 60: 15.0e+6}  # [A]
    T_i_right_bc: {0: 0.1, 60: 0.2}  # edge T_i BC [keV]
    T_e_right_bc: {0: 0.1, 60: 0.2}
    n_e_right_bc: {0: 0.1, 60: 0.25}  # edge density [fGW]
    n_e_right_bc_is_fGW: true
  numerics:
    t_final: 60.0                # ramp-up only [s] → 600 steps
  geometry:
    geometry_configs:
      0.0: {geometry_file: iter_baseline_ip030.eqdsk}
      7.5: {geometry_file: iter_baseline_ip045.eqdsk}    # Ip crosses 4.5 MA
      60.0: {geometry_file: references/iter_baseline_450s.eqdsk}
  sources:
    generic_heat:
      P_total: 2.0e+7
      electron_heat_fraction: 1.0
    ecrh:
      P_total: 0.0
    gas_puff:
      S_total: 6e+21
    generic_particle:
      S_total: 0.0
    pellet:
      S_total: 0.0
actuators:
- {name: P_nbi, low: 0.0, high: 5.0e+7, max_delta: 2.0e+6, init: 2.0e+7}
- {name: P_eccd, low: 0.0, high: 5.0e+7, max_delta: 2.0e+6, init: 0.0}
- {name: rho_eccd, low: 0.0, high: 0.8, max_delta: 0.05, init: 0.3}
- {name: gas_puff_rate, low: 1e+20, high: 1e+22, max_delta: 2e+20, init: 6e+21}
