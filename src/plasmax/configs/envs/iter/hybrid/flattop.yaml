# ITER hybrid — flat-top phase.
#
# The episode begins already in the flat-top burn: Ip held at 10.5 MA and the
# plasma starts from the saved H-mode Bohm-GyroBohm state after 1000 steps
# (100 s). Its resolved profiles and energy history are rounded to four
# significant figures in settled.yaml. Ramp-down uses a separate hot reference.
#
# Geometry:  Static 10.5 MA EQDSK, rescaled to the live Ip. No ramp
#            interpolation. The empirically stable source allocation is
#            documented below. The task spans the full 100--540 s flat-top:
#            t_final = 440 s -> 4400 steps.

phase: flattop
reset_reference: iter_hybrid_settled_bgb
# Backend-independent restart captured from the settled Bohm-GyroBohm state.
# Geometry and each destination backend's transport/solver state are rebuilt at
# load time. The reference manifest fingerprints the selected rounded state.

initialization: ${DATA_DIR}/initializations/iter/hybrid/settled.yaml

task:
  reward: P_diff
  # Maximum auxiliary power is 101 MW, so even a zero-fusion full-horizon
  # trajectory returns about -444 at worst. Keep every physical disruption or
  # solver failure strictly worse than completing that valid trajectory.
  terminal_penalty: -1000

torax:
  profile_conditions:
    Ip: 10.5e+6                  # constant flat-top plasma current [A]
    T_i_right_bc: 0.2            # edge T_i BC [keV]
    T_e_right_bc: 0.2
    n_e_right_bc: 2.5e+19        # exact TORAX v1.4.2 boundary [m^-3]
  numerics:
    t_final: 440.0               # full flat-top control horizon [s] -> 4400 steps
    # Physical resistivity. Values above one are reserved for explicitly
    # unphysical numerical experiments and must not enter task defaults.
    resistivity_multiplier: 1.0
  geometry:
    geometry_file: iter_hybrid_ip105.eqdsk
  sources:
    generic_current:
      fraction_of_total_current: 0.46  # flat-top hybrid fraction (constant)
    generic_heat:
      P_total: 5.1e+7
    ecrh:
      P_total: 0.0
      gaussian_location: 0.13
    gas_puff:
      S_total: 6.0e+21
actuators:
- {name: P_nbi, low: 0.0, high: 5.1e+7, max_delta: 2.0e+6, init: 5.1e+7}
- {name: P_eccd, low: 0.0, high: 5.0e+7, max_delta: 2.0e+6, init: 0.0}
- {name: rho_eccd, low: 0.0, high: 0.8, max_delta: 0.05, init: 0.1274}
- {name: gas_puff_rate, low: 1e+20, high: 1e+22, max_delta: 2e+20, init: 6.0e+21}
